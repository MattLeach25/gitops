apiVersion: apps/v1
kind: Deployment
metadata:
  name: store-front
spec:
  template:
    spec:
      containers:
        - name: store-front
          resources:
            requests:
              cpu: 2m
              memory: 400Mi
